# Find the path where this model tree lives and set a state variable
CBP_ROOT=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

CBP_SRC="$CBP_ROOT/code/src"

CBP_BIN="$CBP_ROOT/run"

export CBP_ROOT CBP_SRC CBP_BIN
