#!/bin/csh
set set FC = $argv[1]
set usrdir = ../../lib
set usrlib = ($usrdir/get_lib.a $usrdir/util_lib.a)

$FC -c main.f
$FC -c getall.f
$FC -c process.f

$FC -o ../../../bin/get_watershed_size.exe main.o getall.o process.o $usrlib

rm *.o
